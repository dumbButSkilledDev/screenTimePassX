# screenTimePassX (UNTESTED)
# I AM NOT RESPONSIBLE FOR DAMAGE (THIS WONT HAPPEN UNLESS U DO SOMETHING DUMB)
# clone the repo, and open a terminal in it and type:
```
pip install -r requirements.txt
python3 sparsepass.py
```